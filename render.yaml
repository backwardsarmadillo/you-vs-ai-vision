services:
  - type: web
    name: vcr-duel
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn vcr_backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: GEMINI_API_KEY
        sync: false
